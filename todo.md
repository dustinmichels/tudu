# Tasks

## Import - Add ability to import tasks from other TODO list apps.

- [x] There is a menu button > import. When clicked it opens a modal showing other providers. (Remember the Milk, Todoist). For now all are greyed out except for Remember the milk
- [x] Use `samples/rememberthemilk_sample.json` as a test. I want to capture everything that is in `opentask-v1.json`. It's okay not to capture absolutely everything.
- [x] Also write a test that `samples/rememberthemilk_sample.json` and asserts that reasonable data is captured.

## Keyboard Shortcuts

- [x] ctrl + tab to move to next task in todo list
- [x] ctrl + shift + tab to move backwards
- [x] "enter" to mark task as completed
- [x] keyboard shortcuts should be documented in a popup modal I can open in app
